const ROLES = {
  ADMIN: 'admin',
  USER: 'user'
};

const PRODUCT_CATEGORIES = [
  'Fashion',
  'Cosmetics'
];

const DEMAND_LEVELS = {
  HIGH: 'high',
  MEDIUM: 'medium',
  LOW: 'low'
};

module.exports = {
  ROLES,
  PRODUCT_CATEGORIES,
  DEMAND_LEVELS
};
